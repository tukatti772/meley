<%# div class="likes"の中身を、指定したhtml要素に置き換える。
置き換えるタグの場所を間違えると、訳が分からないことになるので、
chromeの検証で、htmlをよーく観察すること。 %>
id = <%= @id %>
$('div#classify' + id).html("<%= j(render 'dairyanswer/iine4', iine: @data) %>");