<% if user_signed_in? %>

<%#以下は恐らく、返り値がnilでなければtrue,nilならfalseとなっている。Ruby独特。%>
 <% if iine.like_user(current_user.id) %>
  <%= button_to dairyanswer_like_delete_path(iine), method: :delete, id: :like_button, remote: true do %>
    <%= image_tag 'heart.png', width: 20, height: 20 %>
    いいね！<%= iine.likes_count %>
  <% end %>
 <% else %>
  <%= button_to dairyanswer_like_path(iine), id: :like_button, remote: true do %>
   <%= image_tag 'nonheart.png', width: 20, height: 20 %>
   いいね！<%= iine.likes_count %>
  <% end %>
 <% end %>

<% else %>
 <%= image_tag 'heart.png', width: 20, height: 20 %>
 いいね！<%= iine.likes_count %>

<% end %>
