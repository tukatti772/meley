<% if user_signed_in? %>

<% if var.bookmarkornot(current_user.id,var.id) %>

<%# ブックマーク解除ボタン %>
<%= button_to bookmark_delete_dairy_path, method: :delete, remote: true, class: "postbutton" do %>
ブックマーク中
<% end %>

<% else %>

<%# ブックマークボタン %>
<%= button_to bookmark_dairy_path, method: :post, remote: true, class: "postbutton" do %>
ブックマークする
<% end %>

<% end %>

<% end %>
