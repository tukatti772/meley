<!--検索フォームのテンプレートファイル-->


<p class="a1">
検索結果を以下に表示しています。
</p>

<br>



<% hitnumber.each do |roop| %>

<div class='showbox'>


<%#下記のようなリンク記法は覚えておく。linktoタグではうまくいかなかった。%>
<div id='indextitle'><a href ="/<%= searchlink %>/<%= roop.id %>"><%= roop.title %></a></div>
<br>
<br>

<div class='ellipsis'><%= strip_tags roop.text %></div>
<br>
<br>

<span>
投稿者：<%= link_to "#{roop.name}" , "/users/#{roop.user_id}" %>

<% if kaitousub[roop.id] == nil then %>
<%# == と記述しないと、左辺と右辺が等しいという意味にならないよ！ %>
　　回答数：<span> <%= "0" %> </span>
<% else %>
　　回答数：<span> <%= kaitousub[roop.id] %> </span>
<% end %>

　　投稿日：<%= roop.created_at.strftime('%Y/%m/%d %H:%M') %>
</span>

</div>
<% end %>
