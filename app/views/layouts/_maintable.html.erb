

<% allposts.each do |roop| %>
<div class='showbox'>


<%#下記のようなリンク記法は覚えておく。linktoタグではうまくいかなかった。%>
<div id='indextitle'><a href ="/<%= link %>/<%= roop.id %>"><%= roop.title %></a></div>
<br>
<br>

<div class='ellipsis'><%= strip_tags roop.text %></div>
<br>
<br>

<%# 投稿に紐づいているタグを1つずつ表示する %>
<% roop.tags.each do |yeah| %>
<div class="tag"><%= link_to "#{yeah.tag}" , "/tags/#{yeah.id}", :style => "color:white;"%></div>
<% end %>


<br>

<span>
投稿者：<%= link_to "#{roop.name}" , "/users/#{roop.user_id}" %>

<% if kaitousu[roop.id] == nil then %>
<%# == と記述しないと、左辺と右辺が等しいという意味にならないよ！ %>
　　回答数：<span> <%= "0" %> </span>
<% else %>
　　回答数：<span> <%= kaitousu[roop.id] %> </span>
<% end %>

　　投稿日：<%= roop.created_at.strftime('%Y/%m/%d %H:%M') %>
</span>

</div>
<% end %>

